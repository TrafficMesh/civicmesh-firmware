name: Firmware Build (ARM)

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Cross-compile for ARM
      run: |
        echo "Building firmware for Raspberry Pi CM4..."
